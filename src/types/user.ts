export interface User {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  gender?: string;
  birthDate?: Date;
  address?: string;
  profilePicture?: string;
}
